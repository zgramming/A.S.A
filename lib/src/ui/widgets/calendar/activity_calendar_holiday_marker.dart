import 'package:flutter/material.dart';

class ActivityCalendarHolidaysMarker extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Icon(
      Icons.add_box,
      size: 20.0,
      color: Colors.blueGrey[800],
    );
  }
}
